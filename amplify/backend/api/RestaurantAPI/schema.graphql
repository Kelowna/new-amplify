enum Town {
  TORONTO
  MONTREAL
  VANCOUVER
  CALGARY
  HALIFAX
}

type Restaurant @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String!
  city: String!
  stars: Int
  trouble: Boolean
  traveltown: Town
}
 